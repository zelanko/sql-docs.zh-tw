---
title: S2SS0092：不支援所提供日期格式的轉換（錯誤）
description: 說明為什麼 SQL Server 移轉小幫手（SSMA） for Sybase 不會使用指定的樣式引數來轉換 CONVERT 函數的呼叫。
authors: nahk-ivanov
ms.service: ssma
ms.devlang: sql
ms.topic: article
ms.date: 4/10/2020
ms.author: alexiva
ms.openlocfilehash: 3c93401cd953c47652d68399037f62c916c7adf0
ms.sourcegitcommit: e042272a38fb646df05152c676e5cbeae3f9cd13
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/27/2020
ms.locfileid: "81219551"
---
# <a name="s2ss0092-the-conversion-for-provided-date-format-is-not-supported-error"></a>S2SS0092：不支援所提供日期格式的轉換（錯誤）

本文說明 Sybase 的 SQL Server 移轉小幫手（SSMA）不會將使用指定樣式引數的 CONVERT 函數呼叫轉換成

## <a name="background"></a>背景

每當有`CONVERT`函式呼叫或從日期/時間資料類型呼叫時，SSMA 就會嘗試適當地進行轉譯。 Sybase/SAP ASE 和 SQL Server 支援不同的`format-style`日期和時間格式，因此可能會有（第三個）引數的值，但在 SQL Server 中沒有直接對應。 SSMA 會嘗試使用明確`FORMAT`函式呼叫來模擬其中一些樣式，但仍然可能會有 SSMA 無法轉換的格式樣式。

## <a name="example"></a>範例

請考慮下列範例，我們會嘗試使用樣式`30`來格式化日期值：

```sql
SELECT
  CONVERT(VARCHAR(20), [StartDate], 30) AS [StartDate]
FROM
  [Employees]
```

當您嘗試在 SSMA 中轉換上述程式碼時，它會產生下列錯誤訊息：

> S2SS0092：不支援所提供日期格式的轉換

## <a name="possible-remedies"></a>可能的解決辦法

若要解決此錯誤，您可以`FORMAT`使用具有所需格式的函數：

```sql
SELECT
  FORMAT([StartDate], 'dd \d MM \m yyyy \y') AS [StartDate]
FROM
  [Employees]
```
